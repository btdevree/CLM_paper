function [xcor_mean_vec, xcor_stdev_vec, length_vec] = dots_simulation(params, number_cells, av_dots_per_cell,...
    stdev_dots_per_cell,av_correlation, stdev_correlation, dot_radius, method_flag, dot_precision)
%DOTS_SIMULATION Runs a simulation of finding the correlation between a
%   STORM target and rare, well localized "dots".

end

